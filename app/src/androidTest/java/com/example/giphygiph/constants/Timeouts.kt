package com.example.giphygiph.constants

class Timeouts {
    companion object {
        const val TEST_TIMEOUT_SHORT = Time.ONE_MINUTE * 5
        const val TEST_TIMEOUT_MEDIUM = Time.ONE_MINUTE * 8
        const val TEST_TIMEOUT_LONG = Time.ONE_MINUTE * 10
        const val TEST_TIMEOUT_VERY_LONG = Time.ONE_MINUTE * 15
    }
}